import 'package:flutter/material.dart';

class HomeScreenAndroid extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Android Home")),
      body: Center(child: Text("Hello from Android")),
    );
  }
}
